#include <stdio.h>
/**
 * main- entry point
 * @void: no parameters
 * description: uses the printf function
 * Return: always 0(success)
 */
int main(void)
{
char *alxstr = "with proper grammar, but the outcome is a piece of art,";
printf("%s\n", alxstr);
return (0);
}
