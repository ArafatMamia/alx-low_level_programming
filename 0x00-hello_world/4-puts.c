#include <stdio.h>
/**
 * main -entry point
 * @void: no parameters
 * Description: uses puts to output
 * Return: always 0(success)
 */

int main(void)
{
char *alxstr = "\"Programming is like building a multilingual puzzle";

puts(alxstr);
return (0);
}
